{
  "name": "did-registry-blockchain",
  "version": "0.1.0",
  "description": "DidRegistry smart contract for DID IdP",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "coverage": "hardhat coverage",
    "coverage:js": "c8 --reporter=lcov --report-dir=coverage-js node scripts/deploy.js 2>/dev/null || c8 --reporter=lcov --report-dir=coverage-js --all --include='scripts/**' node -e \"require('./scripts/deploy.js')\" 2>/dev/null; c8 report --reporter=lcov --report-dir=coverage-js || true",
    "deploy:local": "hardhat run scripts/deploy.js --network localhost",
    "node": "hardhat node"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "c8": "^10.1.3",
    "hardhat": "^2.22.0"
  }
}
